///
/// HybridGgwaveSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ProtocolInfo` to properly resolve imports.
namespace margelo::nitro::ggwave { struct ProtocolInfo; }

#include <NitroModules/ArrayBuffer.hpp>
#include <functional>
#include <optional>
#include <string>
#include "ProtocolInfo.hpp"
#include <vector>

namespace margelo::nitro::ggwave {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Ggwave`
   * Inherit this class to create instances of `HybridGgwaveSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridGgwave: public HybridGgwaveSpec {
   * public:
   *   HybridGgwave(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridGgwaveSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridGgwaveSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridGgwaveSpec() override = default;

    public:
      // Properties
      virtual std::optional<std::function<void(const std::shared_ptr<ArrayBuffer>& /* payload */)>> getOnMessage() = 0;
      virtual void setOnMessage(const std::optional<std::function<void(const std::shared_ptr<ArrayBuffer>& /* payload */)>>& onMessage) = 0;
      virtual bool getIsListening() = 0;
      virtual bool getAllowSelfReception() = 0;
      virtual void setAllowSelfReception(bool allowSelfReception) = 0;
      virtual std::string getRoute() = 0;
      virtual double getSamplesPerFrame() = 0;
      virtual std::vector<ProtocolInfo> getProtocols() = 0;
      virtual double getMaxPayloadFixed() = 0;
      virtual double getMaxPayloadVariable() = 0;
      virtual double getMaxInstances() = 0;

    public:
      // Methods
      virtual void start(double protocolId, double payloadLength, bool variableLength, double soundMarkerThreshold) = 0;
      virtual void stop() = 0;
      virtual void send(const std::shared_ptr<ArrayBuffer>& payload, double volume) = 0;
      virtual std::shared_ptr<ArrayBuffer> encode(const std::shared_ptr<ArrayBuffer>& payload, double protocolId, double volume) = 0;
      virtual std::optional<std::shared_ptr<ArrayBuffer>> decode(const std::shared_ptr<ArrayBuffer>& samples) = 0;
      virtual double durationMs(double protocolId, double payloadBytes, bool variableLength) = 0;
      virtual void setFreqStart(double protocolId, double freqStartHz) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Ggwave";
  };

} // namespace margelo::nitro::ggwave
